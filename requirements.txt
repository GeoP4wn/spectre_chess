# Core dependencies
python-statemachine==2.1.2
chess==1.10.0
asyncio==3.4.3
aiosqlite==0.19.0

# Hardware communication (when ready)
pyserial==3.5
pyserial-asyncio==0.6

# Voice recognition (Vosk - install when ready)
# vosk==0.3.45

# Lichess integration
berserk==0.13.2

# Web interface (FastAPI for WebSocket server - coming next)
fastapi==0.109.0
uvicorn==0.27.0
websockets==12.0

# Utilities
python-dotenv==1.0.0
